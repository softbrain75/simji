version: 1
frontend:
  phases:
    build:
      commands:
        - 'echo "window.SIMJI_CONFIG = { apiUrl: \"$SIMJI_API_URL\" };" > config.js'
  artifacts:
    baseDirectory: .
    files:
      - index.html
      - styles.css
      - detail-edit.css
      - media.css
      - app.js
      - config.js
      - manifest.webmanifest
      - service-worker.js
      - icons/simji-icon.svg
  cache:
    paths: []
